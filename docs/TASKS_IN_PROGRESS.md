Правило версий по цветам: 🟢 зелёные задачи = patch `0.0.x`; 🟡 жёлтые задачи = minor `0.x.0`; 🔴 красные задачи = major `x.0.0`. Текущая версия после обновления changelog: v14.8.49.

# TASKS_IN_PROGRESS.md — задания в разработке (Neyronya / Нимфея)

## Правила работы с этим файлом
- Отсюда берутся новые механики для реализации.
- Сделанные задачи удаляются из этого файла (история — в `CHANGELOG.md`).
- Список разбит по простоте: 🟢 простые → 🟡 средние → 🔴 сложные.
- Правило версий по спискам: 🟢 зелёный список = patch-версии `0.0.x`; 🟡 жёлтый список = minor-версии `0.x.0`; 🔴 красный список = major-версии `x.0.0`.

---

## 🟢 Зелёные (patch 0.0.x) — быстро оживить существующее

- [x] Подключить стриминг: `pipeline._respond` → `llm.generate_stream` → резка на предложения → TTS-стрим (`inference_stream`), первый звук ≤2–3 с (см. docs/SKELETON.md §2.1, §4.1) — сделано в v14
- [x] Вшить БАНВОРДЫ: `data/speech_banwords.txt` → маскировка в `tts.clean_for_tts` (редактор в дебаг-меню уже есть) — сделано в v14
- [x] Подключить ТРАССУ ЗАПРОСА/ОЗВУЧКИ: `core/pipeline_trace.py`, этапы in→prompt→llm→tts→play → `logs/pipeline_trace.jsonl` — сделано в v14
- [x] Анти-галлюцинации: «отвечай на последнее, не выдумывай, не знаешь — [ПОИСК]» в промпт + автодедуп ответа с последними 3 — сделано в v14
- [ ] Улучшить референсы TTS: 1 чистый файл 15–30 с, обрезка шумов, loudnorm (требование 12, шаг 1 — нужен чистый файл голоса от пользователя)
- [ ] **тест**

## 🟡 Жёлтые (minor 0.x.0) — новые модули-обвесы

- [x] `stt/`: loopback-канал WASAPI через PyAudioWPatch — слышит собеседника из созвона, метка канала на транскриптах — сделано в v14
- [x] `ears/`: гейт адресации — слышит всегда, отвечает только на «Нимфея/Нима/Ним…» (regex + резервный микро-классификатор LLM); непроадресованное — в память как подслушанное — сделано в v14
- [x] `voice_id/`: speechbrain ECAPA, профили в `people[].voice_embedding`, speaker_id в каждой записи диалога — сделано в v14
- [x] Нити разговора: watchdog хвостов (вопрос без ответа → переспрос; сказано без реакции → повтор с претензией; макс 2, эскалация тона) — сделано в v14
- [x] `web/`: ddgs-поиск по тегу `[ПОИСК: …]` от LLM + правило «спросила — ответа нет N минут → нашла сама» — сделано в v14
- [x] `initiative/`: планировщик тишины (3–5 мин) — случайный факт/история из памяти/тема по контексту; отмена при входе пользователя — сделано в v14
- [x] Память v3: гибридный recall (токены + эмбеддинги Ollama nomic-embed-text, индекс в cache/embeddings.jsonl), `memory.query(кто/когда/текст)` — сделано в v14 (экран в дебаг-меню — backlog)
- [x] VRMA: 7 официальных анимаций VRoid → `vita_avatar_app/animations/` → `@pixiv/three-vrm-animation` в viewer.js → маппинг action/mood → клип, оживить `jump()` — сделано в v14 (jump → VRMA_02)
- [ ] **Тест 2**

## 🔴 Красные (major x.0.0) — фундаментальные

- [ ] Голос 2.0: сравнение F5-TTS против XTTS (RU, RTF, похожесть) в бюджете 6 ГБ VRAM; при победе — замена движка + стриминг
- [ ] Файнтюн клона: запись 5–10 мин целевого голоса → F5-TTS/GPT-SoVITS v2 (отдельное окружение) — нужен голос от пользователя
- [ ] Лора-датасет v2: пересборка из memory.json — сборщик `training/build_dataset.py` готов (v14); прогнать LoRA-обучение по `training/README.md`
- [ ] Экран «ЗАПРОС ПАМЯТИ» в debug menu поверх `memory.query()`
